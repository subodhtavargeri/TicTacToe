workflows:
  my-workflow:
    scripts: 
      - name: TicTacToe Pipeline
        script: |
          xcode-project run-tests \
            --workspace TicTacToe.xcworkspace \
            --scheme TicTacToe
  